// Tailwind CSS v4 is configured CSS-first in src/app/globals.css (@theme blocks).
// This stub exists only so tooling and docker mounts that expect the file keep working.
// Do not add theme configuration here — it is not loaded by the build.
export default {}
